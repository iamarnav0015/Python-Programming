def uppr(s):
    a=""
    for i in s:
        if 97<=ord(i)<=122:
            a+=chr(ord(i)-32)
        else:
            a+=i    
    return a        
b=input("enter string in lower  ")
print(uppr(b))   
